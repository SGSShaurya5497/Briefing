# This directory holds uploaded images for the PoC (local storage mode).
# Files here are generated at runtime and should NOT be committed to git.
# The .gitkeep ensures the directory is tracked by git while its contents are ignored.
